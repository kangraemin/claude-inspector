# Phase 1 Step 1: clipboard.writeText() .catch() 추가

## Test Cases
- [ ] TC1: `copyPayload` 함수에 `.catch(e => console.error('copy failed', e))` 존재
- [ ] TC2: `copyResponse` 함수에 `.catch(e => console.error('copy failed', e))` 존재
- [ ] TC3: `copyExport` 함수에 `.catch(e => console.error('copy failed', e))` 존재
- [ ] TC4: `copyProxyCmd` 함수에 `.catch(e => console.error('copy failed', e))` 존재
- [ ] TC5: `copyProxyDetail` 함수에 `.catch(e => console.error('copy failed', e))` 존재
- [ ] TC6: E2E 테스트 25개 통과 (npm run test:e2e)

## 구현 결과
(구현 후 작성)

## 실제 결과
(테스트 후 작성)
